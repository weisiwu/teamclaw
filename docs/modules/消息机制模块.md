---
title: 消息机制模块
category: 功能模块
created: 2026-03-17
updated: 2026-03-17
---

# 消息机制模块

[← 返回主文档](../系统架构.V1.md)

---

> ⚠️ **数据源要求**：严格禁止使用 mock 数据或伪造数据。所有数据必须来自真实项目仓库（默认 https://github.com/weisiwu/teamclaw ）。数据源可在 [项目导入模块](./项目导入模块.md) 中通过「URL 下载」或「本地上传」方式切换。
>
> 🎨 **样式检测要求**：每个功能在实现过程中，必须检测并验证实际渲染的 UI 样式（布局、间距、配色、响应式等），确保页面展示效果与设计一致，禁止只关注逻辑而忽略样式。

---

## 1. 消息处理规则

| 消息类型 | 处理方式 |
|---------|---------|
| **@Agent 的文本消息** | 触发任务流程 |
| **语音消息** | 暂不支持，忽略 |
| **文件消息** | 存储 + 文件理解（仅支持特定文档类型） |
| **表情消息** | 忽略 |
| **Agent 响应前的消息** | 进入消息队列，立即回复「消息已收到，稍后处理」 |

---

## 2. 消息队列与优先级

### 2.1 优先级计算公式

```
优先级 = 角色权重 × 紧急度

紧急度判定：
  包含"紧急/立刻/马上" → ×3
  普通消息             → ×1
```

### 2.2 示例

| 用户 | 角色 | 权重 | 消息内容 | 紧急度 | 优先级 |
|------|------|------|---------|--------|--------|
| 张三 | 管理员 | 10 | "紧急修复首页Bug" | 3 | 30 |
| 李四 | 副管理员 | 7 | "添加一个按钮" | 1 | 7 |
| 王五 | 普通员工 | 3 | "查看任务列表" | 1 | 3 |

---

## 3. 抢占规则

```
抢占规则：
  新任务优先级 > 当前任务优先级 × 1.5 → 触发抢占
  被抢占任务 → tasks/suspended/ 队列
  抢占时群发通知，告知其他人的任务已被挂起
```

### 3.1 抢占示例

```
当前正在执行：李四的任务（优先级 7）

新消息到达：张三的紧急任务（优先级 30）

判断：30 > 7 × 1.5 (10.5) → 触发抢占

操作：
  1. 暂停李四的任务 → tasks/suspended/
  2. 开始执行张三的任务
  3. 群发通知："李四的任务已被暂停，正在处理张三的紧急任务"
```

---

## 4. 消息合并

同一用户在 **5 分钟内**的连续消息自动合并为一条结构化指令，避免碎片化触发。

### 4.1 合并示例

```
10:00:00 - 用户: "把首页的按钮"
10:00:15 - 用户: "改成蓝色"
10:00:30 - 用户: "字号调大一点"

合并后（10:05:30）：
  "把首页的按钮改成蓝色，字号调大一点"
```

---

## 5. 消息队列数据结构

```json
{
  "queue_id": "q_20260316_001",
  "messages": [
    {
      "message_id": "msg_001",
      "user_id": "user_001",
      "user_name": "张三",
      "role": "admin",
      "weight": 10,
      "content": "紧急修复首页Bug",
      "urgency": 3,
      "priority": 30,
      "timestamp": "2026-03-16T10:00:00+08:00",
      "status": "pending"
    },
    {
      "message_id": "msg_002",
      "user_id": "user_002",
      "user_name": "李四",
      "role": "vice_admin",
      "weight": 7,
      "content": "添加一个按钮",
      "urgency": 1,
      "priority": 7,
      "timestamp": "2026-03-16T10:01:00+08:00",
      "status": "suspended"
    }
  ]
}
```

---

## 6. 文件消息处理

### 6.1 支持的文档类型

| 文档类型 | 处理方式 |
|---------|---------|
| **PPT/PPTX** | 提取文字和图片，转换为 Markdown |
| **Word/DOCX** | 提取文字和表格，转换为 Markdown |
| **Excel/XLSX** | 提取表格数据，转换为 CSV/JSON |
| **PDF** | OCR 识别文字，转换为文本 |
| **图片** | OCR 识别文字，存储原图 |
| **Markdown** | 直接存储 |

### 6.2 文件存储路径

```
~/.openclaw/files/{project_name}/
  ├── documents/     # 文档文件
  ├── images/        # 图片文件
  └── converted/     # 转换后的文件
```

---

## 7. 消息队列管理流程

```
消息到达
    │
    ├── 1. 识别用户身份和角色
    ├── 2. 计算优先级
    ├── 3. 检查是否需要合并
    ├── 4. 入队
    ├── 5. 检查是否需要抢占
    │
    ├── 需要抢占 ──→ 暂停当前任务，执行新任务
    │
    └── 不需要抢占 ──→ 按优先级排队等待
```

---

## 8. API 定义

| 方法 | 路径 | 说明 |
|------|------|------|
| `POST` | `/api/v1/messages` | 接收外部消息（微信/飞书 Webhook） |
| `GET` | `/api/v1/messages/queue` | 获取当前消息队列（含优先级排序） |
| `GET` | `/api/v1/messages/queue/:queueId` | 获取队列详情 |
| `POST` | `/api/v1/messages/queue/:messageId/preempt` | 手动触发抢占 |
| `GET` | `/api/v1/messages/history` | 获取消息历史（支持分页、按用户/时间筛选） |
| `POST` | `/api/v1/messages/file` | 上传文件消息 |

**POST /api/v1/messages 请求示例**

```json
{
  "channel": "wechat",
  "userId": "user_001",
  "content": "紧急修复首页Bug",
  "type": "text",
  "mentionedAgent": "main",
  "timestamp": "2026-03-16T10:00:00+08:00"
}
```

**GET /api/v1/messages/queue 响应示例**

```json
{
  "code": 0,
  "data": {
    "list": [
      {
        "messageId": "msg_001",
        "userId": "user_001",
        "userName": "张三",
        "role": "admin",
        "content": "紧急修复首页Bug",
        "priority": 30,
        "status": "processing",
        "createdAt": "2026-03-16T10:00:00+08:00"
      }
    ],
    "total": 1,
    "currentProcessing": "msg_001"
  },
  "message": "ok"
}
```

---

## 9. 实现任务清单

> 前置依赖：阶段 3（人员权限 + Agent 编排）验收通过。

### 任务 9.1：后端 - 消息接收与队列

| # | 任务 | 产出文件 | 说明 |
|---|------|---------|------|
| 1 | 消息数据模型 | `server/src/models/message.ts` | Message 类型定义，含 channel、priority 等字段 |
| 2 | 消息路由 | `server/src/routes/message.ts` | 上述 6 个 API 端点 |
| 3 | 消息队列服务 | `server/src/services/messageQueue.ts` | 基于 Redis/BullMQ 的优先级队列，入队、出队、排序 |
| 4 | 优先级计算服务 | `server/src/services/priorityCalculator.ts` | `priority = 角色权重 × 紧急度`，紧急关键词检测 |
| 5 | 消息合并服务 | `server/src/services/messageMerger.ts` | 同用户 5 分钟内消息合并为一条结构化指令 |

### 任务 9.2：后端 - 抢占与调度

| # | 任务 | 产出文件 | 说明 |
|---|------|---------|------|
| 1 | 抢占规则引擎 | `server/src/services/preemptionService.ts` | 判断新消息是否触发抢占（> 当前 × 1.5），执行暂停/恢复 |
| 2 | 消息通道适配器 | `server/src/services/channelAdapter.ts` | 微信/飞书消息格式统一转换接口 |
| 3 | 文件消息处理服务 | `server/src/services/fileProcessor.ts` | 文档类型识别、文本提取、存储转换 |

### 任务 9.3：前端 - 消息队列监控页

| # | 任务 | 产出文件 | 说明 |
|---|------|---------|------|
| 1 | 消息队列页面 | `dashboard/src/pages/MessageQueue/index.tsx` | 实时展示队列中的消息，按优先级排序 |
| 2 | 消息卡片组件 | `dashboard/src/pages/MessageQueue/MessageCard.tsx` | 展示用户、内容、优先级、状态，支持手动抢占按钮 |
| 3 | 消息历史页面 | `dashboard/src/pages/MessageHistory/index.tsx` | 消息历史列表，支持按用户/时间筛选 |
| 4 | 消息 API 封装 | `dashboard/src/services/message.ts` | 封装上述 6 个 API |
| 5 | 优先级标签组件 | `dashboard/src/components/PriorityBadge.tsx` | 不同优先级数值对应不同颜色 |

---

## 10. 验收标准

| # | 验收项 | 验证方式 |
|---|--------|---------|
| 1 | `POST /api/v1/messages` 接收消息后正确入队，优先级计算正确 | curl 验证 |
| 2 | 管理员发送"紧急"消息，优先级 = 10 × 3 = 30 | curl 验证 |
| 3 | 高优先级消息正确触发抢占，被抢占任务进入 suspended 状态 | API + 日志验证 |
| 4 | 同用户 5 分钟内连续消息被合并为一条 | API 验证 |
| 5 | 消息队列页面实时展示队列状态，排序正确 | 浏览器截图 |
| 6 | 消息历史页面筛选和分页功能正常 | 浏览器操作 |
| 7 | 文件消息上传后被正确处理和存储 | 文件系统检查 |
| 8 | 所有页面样式正确，优先级标签颜色区分明显 | 浏览器样式检查 |

---

[← 上一章：Agent编排模块](./Agent编排模块.md) | [返回主文档](../系统架构.V1.md) | [下一章：任务机制模块 →](./任务机制模块.md)
