# 20【P2】数据备份与恢复机制

## 任务目标

实现数据库数据的备份与恢复功能。

## 详细说明

### 20.1 备份脚本

创建数据备份脚本 `scripts/backup.ts`：
```typescript
// 导出所有表数据为 JSON
const backup = async () => {
  const dynasties = await supabase.from('dynasties').select('*')
  const poets = await supabase.from('poets').select('*')
  const poems = await supabase.from('poems').select('*')
  // ...
  
  const backupData = { dynasties, poets, poems, tags, poem_tags }
  fs.writeFileSync(`backup-${Date.now()}.json`, JSON.stringify(backupData, null, 2))
}
```

### 20.2 恢复脚本

创建数据恢复脚本 `scripts/restore.ts`：
```typescript
// 从 JSON 导入数据
const restore = async (backupFile: string) => {
  const data = JSON.parse(fs.readFileSync(backupFile, 'utf-8'))
  // 清理现有数据
  // 恢复数据
}
```

### 20.3 Supabase 定时备份

在 Supabase 控制台配置自动备份策略。

## 验收标准

- [ ] 备份脚本正常
- [ ] 恢复脚本正常
- [ ] 数据完整性验证

## 依赖

任务 02、03

## 预计工作量

0.5 人天
